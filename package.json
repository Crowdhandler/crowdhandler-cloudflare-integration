{
  "private": true,
  "name": "crowdhandler-cloudflare-integration",
  "version": "2.0.0",
  "description": "CrowdHandler integration for Cloudflare Workers (ES Modules)",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "lukeowen89 <luke.owen@crowdhandler.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "await-timeout": "^1.1.1"
  }
}
